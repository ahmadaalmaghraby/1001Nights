<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <meta name="theme-color" content="#1e1b4b">
  <meta name="description" content=" ÿ»Ìﬁ √·› ·Ì·… Ê·Ì·… - Ì⁄„· ⁄·Ï «·√‰œ—ÊÌœ">
  <link rel="manifest" href="manifest.json">
  <title>√·› ·Ì·… Ê·Ì·…</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: 'Segoe UI', sans-serif; overflow: hidden; touch-action: pan-y; }
    #app { width: 100vw; height: 100vh; position: relative; background: linear-gradient(to bottom, #1e1b4b, #581c87, #9f1239); overflow: hidden; }
    #canvas { position: absolute; top: 0; left: 0; width: 100%; height: 100%; }
    .header { position: absolute; top: 0; left: 0; right: 0; background: linear-gradient(to bottom, rgba(0,0,0,0.7), transparent); padding: 1.5rem; z-index: 10; }
    .header-content { display: flex; justify-content: space-between; align-items: center; max-width: 1200px; margin: 0 auto; }
    .title { font-size: 1.8rem; font-weight: bold; color: #fcd34d; display: flex; align-items: center; gap: 0.75rem; font-family: serif; text-shadow: 0 0 20px rgba(252, 211, 77, 0.5); }
    .icons { display: flex; gap: 0.75rem; font-size: 1.5rem; }
    .icon { animation: pulse 2s infinite; }
    @keyframes pulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.6; } }
    .stories-container { position: absolute; top: 6rem; left: 1rem; right: 1rem; max-height: 40vh; overflow-y: auto; background: rgba(0,0,0,0.6); backdrop-filter: blur(15px); border-radius: 1.5rem; padding: 1.5rem; border: 2px solid rgba(251, 191, 36, 0.4); z-index: 10; display: none; box-shadow: 0 10px 40px rgba(0,0,0,0.5); }
    .stories-container.show { display: block; animation: slideDown 0.3s ease-out; }
    @keyframes slideDown { from { opacity: 0; transform: translateY(-20px); } to { opacity: 1; transform: translateY(0); } }
    .story-item { margin-bottom: 1rem; padding-bottom: 1rem; border-bottom: 1px solid rgba(251, 191, 36, 0.2); }
    .story-item:last-child { border-bottom: none; margin-bottom: 0; }
    .story-prompt { color: #fcd34d; font-size: 0.95rem; font-weight: bold; margin-bottom: 0.5rem; }
    .story-response { color: rgba(255,255,255,0.95); font-size: 0.9rem; line-height: 1.8; font-family: serif; }
    .response-panel { position: absolute; top: 30%; left: 1rem; right: 1rem; max-width: 700px; margin: 0 auto; background: linear-gradient(135deg, rgba(88, 28, 135, 0.95), rgba(159, 18, 57, 0.95)); backdrop-filter: blur(20px); border-radius: 2rem; padding: 2rem; border: 3px solid rgba(251, 191, 36, 0.6); box-shadow: 0 25px 60px rgba(0,0,0,0.6); z-index: 15; display: none; }
    .response-panel.show { display: block; animation: popIn 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55); }
    @keyframes popIn { 0% { opacity: 0; transform: scale(0.8); } 100% { opacity: 1; transform: scale(1); } }
    .response-header { display: flex; align-items: start; gap: 1rem; margin-bottom: 0.5rem; }
    .response-icon { font-size: 2rem; animation: rotate 3s linear infinite; }
    @keyframes rotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
    .response-title { color: #fcd34d; font-weight: bold; font-size: 1.2rem; margin-bottom: 0.75rem; text-shadow: 0 0 15px rgba(252, 211, 77, 0.5); }
    .response-text { color: white; line-height: 1.9; text-align: right; font-family: serif; font-size: 1rem; }
    .input-area { position: absolute; bottom: 0; left: 0; right: 0; background: linear-gradient(to top, rgba(0,0,0,0.9), transparent); padding: 1.5rem; z-index: 10; }
    .input-container { max-width: 1200px; margin: 0 auto; background: rgba(0,0,0,0.8); backdrop-filter: blur(15px); border-radius: 4rem; border: 3px solid rgba(251, 191, 36, 0.6); padding: 0.75rem; display: flex; align-items: center; gap: 0.75rem; box-shadow: 0 10px 40px rgba(0,0,0,0.5); }
    .input-field { flex: 1; background: transparent; border: none; color: white; padding: 1rem 1.5rem; outline: none; font-size: 1rem; font-family: serif; }
    .input-field::placeholder { color: rgba(251, 191, 36, 0.5); }
    .send-button { background: linear-gradient(135deg, #eab308, #f97316); border: none; border-radius: 50%; width: 3.5rem; height: 3.5rem; display: flex; align-items: center; justify-content: center; cursor: pointer; transition: all 0.3s; box-shadow: 0 6px 20px rgba(234, 179, 8, 0.5); }
    .send-button:active:not(:disabled) { transform: scale(0.9); }
    .send-button:disabled { background: linear-gradient(135deg, #4b5563, #6b7280); cursor: not-allowed; box-shadow: none; }
    .send-button svg { width: 1.75rem; height: 1.75rem; stroke: white; fill: none; stroke-width: 2.5; }
    .loader { border: 3px solid white; border-top-color: transparent; border-radius: 50%; width: 1.75rem; height: 1.75rem; animation: spin 0.6s linear infinite; }
    @keyframes spin { to { transform: rotate(360deg); } }
    .status-text { color: rgba(251, 191, 36, 0.8); font-size: 0.9rem; text-align: center; margin-top: 0.75rem; font-family: serif; }
    ::-webkit-scrollbar { width: 8px; }
    ::-webkit-scrollbar-track { background: rgba(0,0,0,0.3); border-radius: 4px; }
    ::-webkit-scrollbar-thumb { background: rgba(251, 191, 36, 0.6); border-radius: 4px; }
  </style>
</head>
<body>
  <div id="app">
    <canvas id="canvas"></canvas>
    <div class="header">
      <div class="header-content">
        <div class="title"><span>??</span><span>√·› ·Ì·… Ê·Ì·…</span></div>
        <div class="icons"><span class="icon">?</span><span class="icon">?</span></div>
      </div>
    </div>
    <div id="storiesContainer" class="stories-container"></div>
    <div id="responsePanel" class="response-panel">
      <div class="response-header">
        <span class="response-icon">??</span>
        <div style="flex: 1;">
          <div class="response-title">Õﬂ«Ì… ”Õ—Ì… ÃœÌœ…</div>
          <div id="responseText" class="response-text"></div>
        </div>
      </div>
    </div>
    <div class="input-area">
      <div class="input-container">
        <input id="inputField" type="text" class="input-field" placeholder="«ﬂ » Õﬂ«Ì ﬂ «·”Õ—Ì…... ?" dir="rtl">
        <button id="sendButton" class="send-button">
          <svg id="sendIcon" viewBox="0 0 24 24"><path d="M22 2L11 13M22 2l-7 20-4-9-9-4 20-7z"/></svg>
          <div id="loader" class="loader" style="display: none;"></div>
        </button>
      </div>
      <div class="status-text">? Ì⁄„· √Ê‰·«Ì‰ Ê√Ê›·«Ì‰ ?</div>
    </div>
  </div>
  <script>
    if('serviceWorker' in navigator){window.addEventListener('load',()=>{const sw=`const CACHE='v1';self.addEventListener('install',e=>{e.waitUntil(caches.open(CACHE).then(c=>c.addAll(['/'])))});self.addEventListener('fetch',e=>{e.respondWith(caches.match(e.request).then(r=>r||fetch(e.request)))})`;const blob=new Blob([sw],{type:'application/javascript'});navigator.serviceWorker.register(URL.createObjectURL(blob)).catch(()=>{})})}
    const canvas=document.getElementById('canvas'),ctx=canvas.getContext('2d');function resizeCanvas(){canvas.width=window.innerWidth;canvas.height=window.innerHeight}resizeCanvas();window.addEventListener('resize',resizeCanvas);
    const stars=Array.from({length:200},()=>({x:Math.random()*canvas.width,y:Math.random()*canvas.height,size:Math.random()*3,speed:Math.random()*0.8+0.2,opacity:Math.random()*0.7+0.3}));
    let rotation=0,lampGlow=0,glowDir=1;
    function animate(){ctx.fillStyle='rgba(10,10,30,0.12)';ctx.fillRect(0,0,canvas.width,canvas.height);stars.forEach(s=>{const t=Math.sin(Date.now()*0.001+s.x)*0.5+0.5;ctx.fillStyle='rgba(255,255,255,'+(s.opacity*t)+')';ctx.beginPath();ctx.arc(s.x,s.y,s.size,0,Math.PI*2);ctx.fill();s.y+=s.speed;if(s.y>canvas.height){s.y=0;s.x=Math.random()*canvas.width}});const cx=canvas.width/2,cy=canvas.height/2;ctx.save();ctx.translate(cx,cy);ctx.rotate(rotation*0.012);lampGlow+=glowDir*0.015;if(lampGlow>1||lampGlow<0)glowDir*=-1;const gs=120+lampGlow*40,g=ctx.createRadialGradient(0,0,0,0,0,gs);g.addColorStop(0,'rgba(255,215,0,'+(0.5+lampGlow*0.2)+')');g.addColorStop(0.4,'rgba(255,165,0,'+(0.3+lampGlow*0.15)+')');g.addColorStop(1,'rgba(255,215,0,0)');ctx.fillStyle=g;ctx.fillRect(-gs,-gs,gs*2,gs*2);ctx.fillStyle='#FFD700';ctx.shadowColor='rgba(255,215,0,0.9)';ctx.shadowBlur=25;ctx.beginPath();ctx.ellipse(0,0,55,75,0,0,Math.PI*2);ctx.fill();ctx.fillStyle='#FFA500';ctx.shadowBlur=20;ctx.beginPath();ctx.ellipse(0,-28,38,20,0,0,Math.PI*2);ctx.fill();ctx.fillStyle='#FFD700';ctx.shadowBlur=18;ctx.beginPath();ctx.moveTo(-50,-12);ctx.quadraticCurveTo(-72,-18,-78,-7);ctx.lineTo(-65,2);ctx.quadraticCurveTo(-58,-10,-50,-7);ctx.closePath();ctx.fill();ctx.strokeStyle='#FFA500';ctx.lineWidth=9;ctx.shadowBlur=12;ctx.beginPath();ctx.arc(50,0,28,Math.PI*0.3,Math.PI*1.7);ctx.stroke();for(let i=0;i<8;i++){const a=(rotation+i*45)*Math.PI/180,d=100+Math.sin(rotation*0.04+i)*20,sx=Math.cos(a)*d,sy=Math.sin(a)*d-35;ctx.fillStyle='rgba(255,255,150,'+(0.6+Math.sin(rotation*0.08+i)*0.4)+')';ctx.shadowBlur=20;ctx.shadowColor='rgba(255,255,150,0.9)';ctx.beginPath();ctx.arc(sx,sy,4,0,Math.PI*2);ctx.fill()}ctx.restore();rotation++;requestAnimationFrame(animate)}animate();
    const input=document.getElementById('inputField'),btn=document.getElementById('sendButton'),icon=document.getElementById('sendIcon'),load=document.getElementById('loader'),panel=document.getElementById('responsePanel'),txt=document.getElementById('responseText'),stor=document.getElementById('storiesContainer');
    const stories=[],responses=['›Ì ·Ì·… „‰ ·Ì«·Ì √·› ·Ì·… Ê·Ì·…° ﬂ«‰ Â‰«ﬂ  «Ã— €‰Ì Ì”«›— ⁄»— «·’Õ—«¡ «·Ê«”⁄…° Ê›Ì ÿ—ÌﬁÂ ÊÃœ „’»«Õ« ”Õ—Ì«...','Õﬂ  ‘Â—“«œ: ﬂ«‰ Ì«„« ﬂ«‰ ›Ì ﬁœÌ„ «·“„«‰ Ê”«·› «·⁄’— Ê«·√Ê«‰° ﬂ«‰ Â‰«ﬂ ”·ÿ«‰ ⁄ŸÌ„ ÌÕﬂ„ „„·ﬂ… »⁄Ìœ…...','›Ì „œÌ‰… »€œ«œ «·⁄ŸÌ„…° ⁄«‘ ’Ì«œ ›ﬁÌ— «”„Â ⁄»œ «··Â° Ê–«  ÌÊ„ √·ﬁÏ ‘»ﬂ Â ›Ì ‰Â— œÃ·… ›ÊÃœ ﬂ‰“« ⁄ÃÌ»«...','ﬁ«·  ‘Â—“«œ ··„·ﬂ ‘Â—Ì«—: ”„⁄  Ì« „Ê·«Ì √‰ Â‰«ﬂ Ã‰Ì… Ã„Ì·…  ”ﬂ‰ ›Ì ﬁ’— „‰ –Â» Ê”ÿ «·€«»… «·”Õ—Ì…...','›Ì “„‰ «·Œ·Ì›… Â«—Ê‰ «·—‘Ìœ° ﬂ«‰ Â‰«ﬂ ‘«» Ìœ⁄Ï ⁄·«¡ «·œÌ‰° Ê–«  ÌÊ„ ﬁ«»· ”«Õ—« ⁄ÃÌ»« €Ì— „Ã—Ï ÕÌ« Â...','—Ê  ‘Â—“«œ: ›Ì Ã“Ì—… »⁄Ìœ… Ê”ÿ «·»Õ«— «·”»⁄…° ﬂ«‰ Ì⁄Ì‘ ⁄„·«ﬁ ÿÌ» «·ﬁ·» ÌÕ—” ﬂ‰Ê“ «·√Ê·Ì‰...'];
    function addStory(p,r){stories.unshift({prompt:p,response:r});updateUI()}
    function updateUI(){if(stories.length===0){stor.classList.remove('show');return}stor.classList.add('show');stor.innerHTML=stories.map(s=>'<div class="story-item"><div class="story-prompt">'+s.prompt+'</div><div class="story-response">'+s.response+'</div></div>').join('')}
    function showResp(t){txt.textContent=t;panel.classList.add('show');setTimeout(()=>panel.classList.remove('show'),8000)}
    function send(){const p=input.value.trim();if(!p)return;btn.disabled=true;icon.style.display='none';load.style.display='block';setTimeout(()=>{const r=responses[Math.floor(Math.random()*responses.length)];addStory(p,r);showResp(r+'\n\n'+p);input.value='';btn.disabled=false;icon.style.display='block';load.style.display='none'},1500)}
    btn.addEventListener('click',send);input.addEventListener('keypress',e=>{if(e.key==='Enter')send()});
    document.addEventListener('contextmenu',e=>e.preventDefault());
  </script>
</body>
</html>